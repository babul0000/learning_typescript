const userName:string = "babul";
// console.log(userName);

const firstName:string = "babul";
const lastName:string = "hossan";
// const fullName = firstName+ " " +lastName;
const fullName = `my full name is ${firstName + " " + lastName}`
// console.log(fullName);

const name:string = "babul";
const age:number = 25;
const message = `my name is ${name} and my age is ${age}`;
console.log(message.toUpperCase().length);
